# 2026-06-28 실험/시뮬레이션 요약

## 수행 내용
**대응기법 커버리지·오버헤드 시뮬레이션** (논리 연산 수준, 아키텍처 독립).
HAETAE 결함주입 대응기법들을 동일 기준으로 비교: 무방어 / 전체 이중연산 /
Lee 등(JKIISC 2026) 4종 및 통합 / 제안기법 HAETAE-IRV.

## 결함 모델 (7종)
요청 4종(skip, register, bitflip, byteflip) + 추천 추가 3종(zeroing, set_const, loop_abort).
다중비트는 단일결함 모델 범위 밖이라 제외.

## 핵심 결과 (키유출 74개 케이스 중 탐지율)
| 대응기법 | 커버리지 | 상대 오버헤드 |
|---|---:|---:|
| **HAETAE-IRV (제안)** | **82.4%** | **×1.03** |
| 전체 이중연산 | 90.5% | ×2.00 |
| Lee 등 통합(suite) | 64.9% | ×1.06 |
| Lee 등 verify/partial/sanity/seed | 37.8/27.0/6.8/4.1% | ×1.05~1.001 |
| baseline | 0% | ×1.00 |

- IRV가 최고 가성비(이중연산 근접 커버리지를 1/60 비용으로).
- 결함모델별 차이 존재: 이중연산은 일시적 결함 100%지만 0-고정/영속변조 미탐;
  Lee 등 sanity/seed는 0-고정형 시드결함만; IRV는 결함모델 거의 무관(80~86%).

## 산출물(이 폴더)
- `simulation_coverage_report.md` — 정식 보고서(방법론·표·그림·한계).
- `cm_coverage_sim.py` — 시뮬레이터(재현: `python3 cm_coverage_sim.py`).
- `coverage_detail.csv` — 공격점×결함모델 상세(대응기법별 Y/N).
- `coverage_summary.json` — 집계(전체/결함모델별/오버헤드).
- `fig_coverage_vs_overhead.pdf/.png` — 커버리지 vs 오버헤드 산점도.

## 비고 / 다음 할 일
- 본 커버리지는 **논리 모델**(두 논문 메커니즘 기반 규칙). 절대 오버헤드는 **STM32F4 실측** 필요.
- 다음: (예정) STM32F4 전체 서명 + 오버헤드 실측, 하드웨어 z1→s1 키복원 재현(F3).
