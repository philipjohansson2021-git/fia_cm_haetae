# PROMPT — `analyst`

글로벌 `PROMPT.md`(게이트 G1–G10) · `ARTIFACT_CONTRACTS` §3 · 본 `ROLE.md`.

1. To_Do 답변 칸 · curator `ready` · MILESTONES에서 **다음 미승인 1단위**.
2. READING/REFS에 `draft` 분석 기록 → 원문·해석·불확실을 To_Do 1건 제시.
3. 사용자 승인 후 분석을 `approved`, producer handoff를 `ready`로 전환.
4. 자료 부족 → curator handoff. Marp 직접 수정 금지.
5. 레거시 노트 재사용 시 근거 필드 보강.

진행 포인터(연구): curator의 DIL-10 자산 패킷 + sparse-c-NTT 사용자 게이트 후 DIL-10 — `MILESTONES`/`To_Do` 따름.
